import React from 'react';
import { render, cleanup } from '@testing-library/react';


describe('App component', () => {
  afterEach(() => cleanup());
  it('renders App', () => {
  expect(0).toEqual(0)
  });
});
